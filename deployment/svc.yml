apiVersion: v1
kind: Service
metadata:
  name: my-profile-svc-${BUILD_ENV}
  labels:
    app: my-profile-${BUILD_ENV}
spec:
  type: NodePort
  ports:
    - port: 4000
      nodePort: ${PORT_NUMBER}
      protocol: TCP
  selector:
    app: my-profile-${BUILD_ENV}
